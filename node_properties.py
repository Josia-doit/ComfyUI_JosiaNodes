"""
Josia_Nodes 所有节点的常量配置
整合：缓存清理、图像缩放、图像对比、组控制器的配置项
"""
from PIL import Image

# ===================== 缓存清理节点配置 =====================
NODE_CLASS_NAME = "JosiaCacheCleanup"
NODE_DISPLAY_NAME_CACHE = "Josia缓存清理"
NODE_CATEGORY = "Josia"  # 所有节点统一分类

DEFAULT_SETTINGS = {
    "clean_vram": True,
    "clean_file": True,
    "clean_process": True,
    "system_procs": {
        "system", "svchost", "explorer",
        "winlogon", "csrss", "lsass"
    }
}

PARAM_DESCRIPTIONS = {
    "clean_vram": "释放推理产生的临时显存缓存（不卸载模型）",
    "clean_file": "释放Windows系统文件缓存，提升系统流畅度",
    "clean_process": "释放非系统进程空闲内存，降低内存占用"
}

# ===================== 图像缩放节点配置 =====================
NODE_DISPLAY_NAME_SCALING = "Josia图像缩放"  # 图像缩放节点中文名

# 四个常用比例尺寸配置（修改后：每栏包含横竖双方向，保留原有尺寸数值）
PRESET_SIZES = {
    "1:1 正方形": [
        ("关", None),
        ("512×512", (512, 512)),
        ("640×640", (640, 640)),
        ("768×768", (768, 768)),
        ("1024×1024", (1024, 1024)),
        ("1280×1280", (1280, 1280)),
        ("1536×1536", (1536, 1536)),
    ],
    "2:3/3:2 摄影比例": [
        ("关", None),
        # 2:3 竖屏（保留你原有尺寸）
        ("512×768 (2:3)", (512, 768)),
        ("640×960 (2:3)", (640, 960)),
        ("768×1152 (2:3)", (768, 1152)),
        ("1024×1536 (2:3)", (1024, 1536)),
        ("1280×1920 (2:3)", (1280, 1920)),
        ("1536×2304 (2:3)", (1536, 2304)),
        # 3:2 横屏（保留你原有尺寸）
        ("768×512 (3:2)", (768, 512)),
        ("960×640 (3:2)", (960, 640)),
        ("1152×768 (3:2)", (1152, 768)),
        ("1536×1024 (3:2)", (1536, 1024)),
        ("1920×1280 (3:2)", (1920, 1280)),
        ("2304×1536 (3:2)", (2304, 1536)),
    ],
    "3:4/4:3 短视频比例": [
        ("关", None),
        # 补充3:4竖屏（适配短视频场景，尺寸为你原有逻辑扩展）
        ("768×1024 (3:4)", (768, 1024)),
        ("960×1280 (3:4)", (960, 1280)),
        ("1080×1440 (3:4)", (1080, 1440)),
        ("1536×2048 (3:4)", (1536, 2048)),
        # 4:3横屏（适配平板/老设备）
        ("1024×768 (4:3)", (1024, 768)),
        ("1280×960 (4:3)", (1280, 960)),
        ("1440×1080 (4:3)", (1440, 1080)),
        ("2048×1536 (4:3)", (2048, 1536)),
    ],
    "16:9/9:16 全平台视频": [
        ("关", None),
        # 16:9 宽屏（保留你原有尺寸）
        ("896×504 (16:9)", (896, 504)),
        ("1280×720 (16:9)", (1280, 720)),
        ("1920×1080 (16:9)", (1920, 1080)),
        ("2560×1440 (16:9)", (2560, 1440)),
        ("3840×2160 (16:9)", (3840, 2160)),
        # 9:16 竖屏（补充手机短视频尺寸）
        ("504×896 (9:16)", (504, 896)),
        ("720×1280 (9:16)", (720, 1280)),
        ("1080×1920 (9:16)", (1080, 1920)),
        ("1440×2560 (9:16)", (1440, 2560)),
        ("2160×3840 (9:16)", (2160, 3840)),
    ]
}

# 锁定倍数下拉选项
LOCK_MULTIPLE_CHOICES = ["关", "2", "4", "8", "16", "32", "64", "128"]

# 裁剪方式选项
CROP_METHODS = ["中心裁剪", "对齐上边", "对齐下边", "对齐左边", "对齐右边", "拉伸缩放"]

# 缩放算法映射（area对应最快的BOX滤波）
RESAMPLE_FILTERS = {
    "nearest-exact": Image.Resampling.NEAREST,
    "bilinear": Image.Resampling.BILINEAR,
    "area": Image.Resampling.BOX,
    "bicubic": Image.Resampling.BICUBIC,
    "lanczos": Image.Resampling.LANCZOS
}

# 数值参数默认值与范围限制
DEFAULT_PARAMS = {
    "scale_length": 1024,
    "pixel_million": 1.00,
    "lock_multiple": "8",
    "resolution_steps": 1,
    "min_scale_length": 32,
    "max_scale_length": 4096,
    "min_pixel_million": 0.01,
    "max_pixel_million": 4.0,
    "min_steps": 1,
    "max_steps": 3,
    "manual_width": 1024,
    "manual_height": 1024,
    "min_manual_size": 32,
    "max_manual_size": 4096,
    "swap_wh": False,
    "default_base_width": 1024,
    "default_base_height": 1024,
    "max_total_pixels": 4_000_000,  # 400万像素封顶
    "default_resample_algo": "area"  # 默认缩放算法：最快的area
}

# ===================== 图像缩放节点描述 =====================
IMAGE_SCALING_DESCRIPTION = """📐 Josia 图像缩放
多功能图像缩放裁切节点，支持 4 大比例预设与 3 种自定义模式。

• 🖼️ 像素缩放：按百万像素目标自动计算尺寸（最高优先级）
• ✏️ 手动宽高：直接指定宽高，支持一键切换宽高
• 📏 边长缩放：按最长边 / 最短边缩放至指定像素
• 预设比例：1:1 / 2:3 / 3:4 / 16:9（含横竖双方向）

⚙️ 高级特性：锁定倍数 / 多种裁剪方式 / 5 种缩放算法 / 分辨率保护
❗ 不设置任何尺寸时，图像原封不动透传；无图像输入时可提供宽高数值用于文生图"""

# ===================== 图像对比节点配置 =====================
IMAGE_COMPARER_DESCRIPTION = """🖼️ Josia 图像对比
快速对比两张图像，支持两种交互模式。

• Slide 模式（默认）：鼠标左右移动出现分界线，左A右B
• Click 模式：按住显示 Image B，松开恢复 Image A

使用方法：连接 image_a 与 image_b，拉大节点即可舒适对比。"""

IMAGE_COMPARER_TOOLTIPS = {}

# ===================== 组控制器节点配置 =====================
DESCRIPTION_M = """\
Josia多组控制 — 批量控制工作流中所有编组。

【全局按钮】
  全部跳过 — 批量将所有编组内的节点设为跳过（Bypass）
  全部启用 — 批量将所有编组内的节点恢复为启用状态

【编组控制行】
  开关按钮 — 单独切换该编组内所有节点的跳过 / 启用状态
  编组名称 — 点击可将画布视图跳转到该编组位置

【右键菜单】
  跳过所有编组 / 启用所有编组

【状态说明】
  绿色「已启用」    编组内所有节点正常执行
  红色「已跳过」    编组内所有节点被跳过，输出透传
  橙色「部分跳过」  编组内节点状态不一致

节点自动识别工作流中的所有编组，无需手动配置。\
"""

DESCRIPTION_S = """\
Josia单组控制 — 精确控制工作流中的单个编组。

【使用方法】
  点击下拉框   — 选择要控制的目标编组
  点击开关按钮 — 切换该编组内所有节点的跳过 / 启用状态

【右键菜单】
  跳过该编组 / 启用该编组

【状态说明】
  绿色「已启用」    编组内所有节点正常执行
  红色「已跳过」    编组内所有节点被跳过，输出透传
  橙色「部分跳过」  编组内节点状态不一致

选中的编组名称会随工作流保存，重新打开后自动恢复。\
"""
